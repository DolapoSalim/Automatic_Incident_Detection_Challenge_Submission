DATASET ANNOTATIONS FILE FORMAT

The annotations file is provided in CSV format and uses a semicolon (;) as the field separator.

When reading the values contained in each cell, it is necessary to trim any leading and trailing whitespace in order to obtain the actual value of each field correctly.